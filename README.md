# WB → УПД

Утилита для российских ИП, продающих товар на **Wildberries** по схеме
выкупа (когда WB выкупает товар у продавца под свою реализацию в странах СНГ и
присылает **Уведомление о выкупе**).

Раз в неделю (или по нажатию кнопки):

1. Забирает из WB API уведомления о выкупе
2. Парсит xlsx из архива, считает суммы и НДС
3. Генерирует **УПД** в формате ФНС 5.03 — файл, готовый к загрузке в Диадок / СБИС / Такском
4. Складывает все XML в одну папку и умеет отдать их одним zip-архивом

> ⚠️ **УПД загружаются в Диадок вручную** (перетаскиванием в веб-кабинет).

## Что это НЕ делает

- Не подписывает XML электронной подписью (подпишешь в Диадоке при загрузке).
- Не ходит в ФНС напрямую.

## Установка

Нужен **Python 3.9+**.

## 1. Запускаем Терминал и 

## 2. Вставляем код (тем самым скачиваем и устанавливаем проект)

```bash
git clone https://github.com/takeshymsi-cpu/wb-upd.git
cd wb-upd

python3 -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

## 3. Запускаем программу

```bash
streamlit run app.py
```

> 👋 Welcome to Streamlit!
>
> If you’d like to receive helpful onboarding emails, news, offers, promotions,
> and the occasional swag, please enter your email address below. Otherwise,
> leave this field blank.
>
> Email:  

Перевод
>👋 Добро пожаловать в Streamlit!
>
>Если вы хотите получать полезные электронные письма о начале работы, новости, предложения, акции,
>и иногда сувениры, пожалуйста, введите свой адрес электронной почты ниже. В противном случае,
>оставьте это поле пустым.

После ввода Email 

Откроется http://localhost:8501.

## Настройка

### 1. Токен WB API

В кабинете продавца WB: **Настройки → Доступ к API** → создай токен с правом
**«Документы»** и скопируй JWT-строку.

Есть два способа его прописать:

- **Через интерфейс (проще)** — в сайдбаре слева в блоке «🔑 Подключение к WB»
  вставь токен и нажми «✅ Подключиться». Галочка «Сохранить в .env» запомнит
  его между запусками.
- **Вручную через `.env`** — скопируй `.env.example` в `.env` и впиши токен:
  ```bash
  cp .env.example .env
  # открой .env в любом редакторе, впиши WB_API_TOKEN=<твой_токен>
  ```

### 2. Реквизиты

На вкладке «Настройки» в UI заполни:

- реквизиты ИП (ФИО, ИНН, ОГРНИП, юр адрес)
- банк (Наименование, БИК, р/с, корсчёт)
- налоговый режим и ставка НДС
- подписант (ИП ФИО)
- данные покупателя — по умолчанию **ООО РВБ** (Wildberries), трогать не надо

Всё сохраняется локально в `settings.yaml`. Файл **не коммитится** в git.

## Использование

1. Вкладка **«Уведомления о выкупе»**:
   - выбираешь дату «С», по умолчанию — 1 января текущего года;
   - жмёшь **«⚡ Сформировать УПД для всех новых»** — прогоняет все
     необработанные уведомления;
   - **«📦 Скачать все»** — забирает все сгенерированные XML одним zip-архивом.

2. В Диадоке: «Создать документ» → «Загрузить из файла» → перетаскиваешь XML →
   подписываешь КЭП → отправляешь контрагенту.

3. Вкладка **«Журнал»** — история всех обработанных уведомлений.

## Где что лежит

| Файл / папка | Назначение |
|--------------|------------|
| `app.py` | Streamlit UI |
| `wb_client.py` | Клиент WB API |
| `xlsx_parser.py` | Парсер xlsx из архива уведомления |
| `upd_builder.py` | Генератор XML УПД (ФНС 5.03, СЧФДОП) |
| `config.py` | Схема настроек + чтение/запись `settings.yaml` |
| `storage.py` | SQLite-журнал обработанных документов |
| `.env` | Токен WB (**локально, не в git**) |
| `settings.yaml` | Реквизиты (**локально, не в git**) |
| `storage.db` | Журнал (**локально, не в git**) |
| `output/` | Сгенерированные XML УПД |

## Безопасность

- Токен WB и реквизиты хранятся **только у тебя на машине**, никуда не уходят.
- Инструмент ходит только к `common-api.wildberries.ru` и
  `documents-api.wildberries.ru`.
- Код открытый — можешь проверить сам.

## Лицензия

MIT — см. `LICENSE`. Используется «как есть», автор не гарантирует
соответствие конкретному формату конкретного оператора ЭДО. Перед первой
боевой отправкой **обязательно** прогоните XML через валидатор Диадока
(или другого оператора) и сверьтесь с актуальным форматом ФНС.
